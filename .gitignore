.DS_Store
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
results/
*.env

# Sub-agent が保存する調査結果の要約。実行のたびに増える生成物。
outputs/

# 手元だけの上書き設定。組織ポリシーで使えるモデルが異なる場合などに使う。
.claude/settings.local.json
**/.claude/settings.local.json

# このリポジトリで公開するのは Kiro 版のみ。手元の作業ディレクトリは追跡しない。
claude-code/
codex/

# agentcore create が reproduce/ の下に生成するプロジェクト。CDK の依存
# (node_modules) とデプロイ ログを含むためリポジトリには入れない。
reproduce/reproduce/
